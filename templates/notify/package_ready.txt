{#- SMS. This is the one message a candidate gets — it has to carry the trust claim. -#}
Verify: your take-home is checked and ready. It doesn't read your SSH keys, cloud creds or browser data, and doesn't run code on install. Signed proof: {{ verify_url }}
